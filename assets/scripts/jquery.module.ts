// NOTE: We don't import jQuery here because we want to keep it separate from our own code. This way
//   we can use the minified jQuery version but still keep our own JavaScript/TypeScript code un-minified (for readability).
// @ts-ignore
//import $ from "../node_modules/jquery/dist/jquery.js";

export default <JQueryStatic>$;
